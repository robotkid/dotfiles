---

- defaults:
    link:
        relink: true
        force: true

- clean: ['~']

- link:
    ~/.config/fish: config-fish/fish
    ~/.config/fisher: config-fish/fisher
    ~/.vimrc: dotfiles-vim/vimrc
    ~/.vim: dotfiles-vim/vim
